# Vault-Audit — pinned dependencies
# Pin to major versions to avoid breaking changes (sklearn 1->2, pydantic 1->2, etc.)

# Web framework
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0

# Database
pymongo>=4.6,<5.0

# ML
scikit-learn>=1.4,<2.0
joblib>=1.3,<2.0
numpy>=1.26,<3.0

# Data generation (db_setup.py)
faker>=22.0,<40.0

# Testing
pytest>=7.4,<9.0
requests>=2.31,<3.0
httpx>=0.27,<1.0          # required by fastapi.testclient